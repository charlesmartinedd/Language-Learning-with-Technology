---
// Re-export BaseLayout as Layout for compatibility
import BaseLayout from './BaseLayout.astro';

export interface Props {
  title: string;
  description?: string;
  image?: string;
}

const props = Astro.props;
---

<BaseLayout {...props}>
  <slot />
</BaseLayout>
